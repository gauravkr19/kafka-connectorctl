apiVersion: connectorctl.io/v1alpha1

connectors:
  postgres-poc:
    fields:
      database.user:
        profile: mounted-file
        secretName: postgres-credentials
        secretKey: username

      database.password:
        profile: mounted-file
        secretName: postgres-credentials
        secretKey: password